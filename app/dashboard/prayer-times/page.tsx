import { PrayerSchedule } from '@/features/prayer-times/prayer-schedule';

export const metadata = { title: 'Prayer Times' };

export default function PrayerTimesPage() {
  return <PrayerSchedule />;
}
